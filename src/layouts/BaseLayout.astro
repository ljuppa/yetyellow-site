---
import '../styles/global.css';

interface Props {
  description?: string;
  title?: string;
}

const {
  description = 'Yetyellow is a personal workbench for practical agentic systems in complex engineering work.',
  title = 'Yetyellow | Practical agentic systems',
} = Astro.props;

const currentPath = Astro.url.pathname;
const navigation = [
  { href: '/', label: 'Home' },
  { href: '/now', label: 'Now' },
  { href: '/about', label: 'About' },
];
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={description} />
    <meta name="theme-color" content="#0c0c0a" />
    <title>{title}</title>
  </head>
  <body>
    <a class="skip-link" href="#content">Skip to content</a>
    <header class="site-header">
      <a class="brand" href="/" aria-label="Yetyellow home">
        <span class="fruit-mark" aria-hidden="true"><span></span></span>
        <span>yetyellow</span>
      </a>
      <nav aria-label="Primary navigation">
        <ul class="nav-list">
          {navigation.map((item) => (
            <li><a href={item.href} aria-current={currentPath === item.href ? 'page' : undefined}>{item.label}</a></li>
          ))}
          <li><a class="nav-contact" href="/contact">Contact</a></li>
        </ul>
      </nav>
    </header>
    <main id="content"><slot /></main>
    <footer class="site-footer">
      <span>YETYELLOW / INDEPENDENT WORKBENCH</span>
      <span>STATUS: FOUNDATION IN PROGRESS</span>
    </footer>
  </body>
</html>
